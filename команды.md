Для запуска приложения в текущей конфигурации (где Frontend настроен на порт 5000) выполните следующие команды в двух разных терминалах:

Терминал 1: Бэкенд (API Сервер)

.\venv\Scripts\python.exe rag/rag_api_server.py
Дождитесь сообщения: 🚀 RAG API Server запущен на http://localhost:5000

Терминал 2: Фронтенд (Интерфейс)
npm run dev
Откройте ссылку, которую покажет Vite (обычно http://localhost:5173/ или http://localhost:3000/)


npm run tauri:dev




taskkill /F /IM python.exe /IM rag_api_server.exe /IM SHUKABASE.exe /IM app.exe /T
Remove-Item -Recurse -Force dist, build, src-tauri/target -ErrorAction SilentlyContinue
.\build_multilingual.bat



(
1. npm run dev (Только Фронтенд)
Эта команда запускает только сайт (Vite сервер).

Где открывается: В обычном браузере (Chrome, Edge).
Что работает: Только внешний вид (HTML/CSS/JS).
Что НЕ работает:
Все функции Tauri (системные меню, работа с окнами).
Бэкенд (Python) сам НЕ запустится.
Поиск работать не будет (если ты отдельно руками не запустила python rag_api_server.py во втором терминале).
Зачем нужно: Если ты верстаешь кнопочки и перекрашиваешь цвета. Это супер-быстро, не ждешь сборки Rust.
2. npm run tauri:dev (Всё вместе) 🚀
Эта команда запускает полноценное приложение.

Запускает npm run dev (фронтенд).
Компилирует Rust-код (системную оболочку).
Автоматически запускает Python-сервер (твой RAG).
Открывает всё это в отдельном окне приложения (как настоящий exe).
Зачем нужно: Для полноценной разработки и проверки функционала. Именно это тебе нужно запускать, чтобы проверить, как работает поиск и ассистент.
Вердикт: Используй npm run tauri:dev. Это правильный способ увидеть приложение таким, каким оно будет у пользователя.)