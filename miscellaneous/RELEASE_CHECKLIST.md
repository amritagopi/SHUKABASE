# 🚀 ЧЕК-ЛИСТ: Как выпустить обновление Shukabase

Этот файл — твоя шпаргалка. Если ты добавляешь новые книги или меняешь код, просто иди по пунктам сверху вниз.

---

## ШАГ 1: Добавление новых книг 📚

1.  **Положи файлы:**
    Скопируй `.txt` файлы новых книг в папку:
    `c:\Users\annac\shukabase-ai\доп книги для шуки\`

2.  **Важно! Приоритетные книги (Core Books):**
    Если книга фундаментальная (для новичков, важные учебники) и должна искаться в первую очередь:
    *   Открой `rag/rag_engine.py`.
    *   Найди список `CORE_BOOKS` (внутри функции `search`).
    *   Добавь туда часть названия файла новой книги.
    *   *Пример:* `'Introductory-handbook'`.

3.  **Обнови базу данных (RAG):**
    Запусти в терминале команду, которая сама всё нарежет, создаст эмбеддинги и индексы:
    ```powershell
    python update_rag_db.py
    ```
    *(Дождись сообщения "SUCCESS: RAG Database Updated!")*

---

## ШАГ 2: Создание архивов с данными 📦

Так как база изменилась, нужно упаковать её для пользователей.

1.  **Запусти сборщик архивов:**
    ```powershell
    powershell -ExecutionPolicy Bypass -File build_data_archives.ps1
    ```
    *Это создаст 3 zip-файла в папке `builds\data_archives\`.*

2.  **Загрузи на GitHub (Репозиторий Данных):**
    *   Иди в репозиторий `shukabase-install-data`.
    *   Создай новый Release (например, `data-v3`).
    *   Загрузи туда полученные zip-файлы.
    *   **СКОПИРУЙ ССЫЛКИ** на каждый из трех файлов.

---

## ШАГ 3: Обновление кода (Связь с данными) 🔗

Теперь нужно сказать приложению, где брать новые данные.

1.  **Открой файл:** `rag/rag_api_server.py`
2.  **Обнови ссылки:**
    Найди словарь `DATA_URLS` и вставь туда новые ссылки, которые ты скопировала в Шаге 2.
3.  **❗ ПОВЫСЬ ВЕРСИЮ ДАННЫХ ❗ (Критично):**
    В этом же файле найди переменную:
    ```python
    DATA_VERSION = 4  # <-- Поменяй на 5 (или следующую цифру)
    ```
    *Если это не сделать, приложение у пользователей НЕ поймет, что нужно качать новую базу.*

---

## ШАГ 4: Повышение версии приложения 🆙

Чтобы пользователи увидели, что вышел новый `.exe`.

1.  **Открой файл:** `src-tauri/tauri.conf.json`
2.  **Найди строку:** `"version": "0.2.0"`
3.  **Поменяй на следующую:** (например, `"0.2.1"`).

---

## ШАГ 5: Сборка и Релиз 🚀

Всё готово, собираем "экзешник".

1.  **Очистка (на всякий случай):**
    В терминале быстро удали временные файлы (если умеешь), или просто надейся на скрипт сборки.

2.  **Запуск сборки:**
    ```powershell
    .\build_multilingual.bat
    ```
    *(Это займет пару минут. Иди попей чайку ☕)*

3.  **Публикация:**
    *   Забери готовый файл (обычно `src-tauri/target/release/SHUKABASE.exe` или переименованный скриптом установщик).
    *   Иди в **основной репозиторий приложения**.
    *   Создай новый Release (с тем же номером, что в Шаге 4, например `v0.2.1`).
    *   Загрузи `.exe`.
    *   Напиши пользователям: "Обнова готова! Качайте, база обновится сама".

---

### 🚨 Экстренная помощь

*   **Если что-то сломалось при сборке:**
    Проверь, что ты не забыла запятую в JSON файлах (`tauri.conf.json`). Это самая частая ошибка.
*   **Если пользователи говорят "База не качается":**
    Проверь `DATA_VERSION` в `rag_api_server.py` — ты точно увеличила цифру?
